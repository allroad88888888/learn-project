---
name: learn-project
description: "学会一个仓库，并把理解留在仓库里而不是聊天窗口里。从代码和 git 历史追出项目的主线与分支线（数出来的证据、file:line 引用、标准/另一类/漂移三分），把少量问题交给负责人确认，之后每次任务只读 1–2 条已确认的线，不再重读整个仓库。触发：接手一个仓库、「学一下这个项目」「这个项目怎么设计的」「X 该放哪」、给 agent 做项目上手、检查一次改动是否还在设计上、很多提交之后查漂移。English: SKILL.md."
---

# learn-project（中文版）

> 英文版 `SKILL.md`，同一套流程。本文件是中文对话时的入口。

一个仓库一定有一条**主线**（每个请求从入口到出口的必经之路）和几条**分支线**（挂在主线某个
汇合点上的、形状相同的可插拔成员家族）。后期改动很少碰它们；它们正是新人最需要、却没人写下来
的东西。这个 skill 让 agent **从代码和 git 历史**把它们挖出来，证据是数出来的，交给负责人一份
很短的问题清单，把确认后的结果**存进仓库**——关窗口、开新会话、换模型都不丢。之后每个任务
只读 1–2 个线文件（每个 < 120 行），不读代码库。

它不是记忆插件、不是聊天压缩、不是向量库、也不是一份每次全读的大 `ARCHITECTURE.md`。
纯 markdown + shell 脚本，任何能加载 skills 的 agent 都能用。

## 词汇

| 词 | 含义 | 机械特征 |
|---|---|---|
| **线** | 一条端到端、设计稳定的路径：入口 → 各步 → 出口，谁管什么，怎么加一个 | — |
| **主线** | 每个实例都要走的脊柱 | 单例（无同形兄弟）、fan-in 高、在入口出发的路径上 |
| **分支线** | 挂在主线上的同形成员家族 | ≥5 个同形兄弟目录 **+ 一个汇合点**（引用大多数成员的那个文件） |
| **公共层** | 共享工具——*不是*线 | fan-in 高、无汇合点、无统一形状 |
| **样板成员** | 该模仿的那个 | 奠基成员（git 里最早）或最近且干净的成员，且守形状 |
| **配方** | 加一个成员要碰的文件 | ≥3 次「成员出生」提交触碰集的交集 |
| **另一类** | 同目录、不同机制 | 不合形状且汇合点不引用它 |
| **漂移** | 同机制、走歪了 | 少、晚、不合形状；负责人标「别模仿」 |
| **裁决** | 负责人对一条问题的回答 | 折回线文件，注明日期与人 |

## 产出放哪

默认 `<repo>/.claude/skills/project-lines/`（Claude Code 每个会话自动加载其中的 `SKILL.md`
索引；其它 agent 把索引放到它自动加载的位置，如从 `AGENTS.md` 引用）。要保持 agent 中立就用
`.project-lines/`。

```
project-lines/
├── SKILL.md            索引——默认只加载这一份（templates/zh/index.md）
├── lines/              00-主线-*.md、01-主线-*.md、10-分支-*.md …（templates/zh/line.md），每份 ≤ 300 行
├── questions.md        给负责人的待确认（templates/zh/questions.md）
└── mechanical/         scripts/extract-all.sh 的提取基线——一起提交；check.sh 对着它 diff
```

全部随代码进 git：线的改动出现在 PR 里、纠正就是 diff、「只有负责人能改设计」靠 review 而不靠工具。

## 流程 A — 学习（每个仓库一次；有意重投入，全队复用）

1. **定范围**——只看 `git ls-files`（永远别用 `ls`：工作区常有别的分支留下的未跟踪残留）。
   记下语言、入口、workspace 清单、测试/构建命令、已有文档（`README`、`CLAUDE.md`、各种清单、
   `.cursor/rules`）——**是待核实的线索，不是权威**。
2. **机械提取**——`bash scripts/extract-all.sh <repo> <out>/`（几秒到一两分钟，零 token）。
   读 `<out>/SUMMARY.md`：家族及成员数与核心文件、每族最高汇合点及覆盖率、import 方向矩阵；
   每族的 `hubs-*`、`timeline-*`、`recipe-*`；脊柱候选的 `churn-*`。细节见 `references/zh/extractors.md`。
3. **定候选线**——主线：从每个入口沿 fan-in 最高、churn 最低的单例走；分支线：家族 + 覆盖率 ≥60% 的
   汇合点；公共层：有 fan-in 无汇合点（写进索引，不追）。把候选清单告诉用户，没人反对就继续（别卡住）。
4. **追线，一线一个子 agent，并行**——prompt 用 `templates/zh/trace-prompt.md`，填入该线的机械
   证据。每个子 agent 读样板成员 + 少数几个 + 全部离群成员（不是全部成员），引 `file:line`，
   数而不猜，分标准/另一类/漂移，写出文档与代码不一致处，提 ≤5 条问题、每条两种解释，写
   `lines/NN-*.md`（≤300 行），汇报打开的文件数。预期每线 ~60–90 个文件、~150k tokens。
5. **合并**——你自己做，不派子 agent：(a) **交叉印证**：≥2 条线独立得出的事实，升格进索引的
   「结构修正」（如「目录上四族，装配上其实两族」）；线之间的冲突变成问题；(b) 跨线去重问题；
   (c) 按 `references/zh/question-filter.md` 过滤（代码能答的不问、不改变新代码去向的不问、
   点名成员、一问一事、允许「都有」和自由文本）；(d) 用 `templates/zh/index.md` 写 `SKILL.md`
   索引，含机械证据、交叉印证清单、成本表；(e) 写 `questions.md`；(f) 把 `<out>/mechanical/`
   拷进目标目录。
6. **确认**——把 `questions.md` 交给负责人。回答词汇：**A / B / 都有 / 就这样 / 遗留 / 不知道 /
   自由文本**。每条答案折回线文件的「裁决」节（`references/zh/verdicts.md`）；确认的规则升格进
   索引；无人确认的线标为不进默认索引（支持的话用 `hidden` frontmatter）。
7. **汇报**——产出了什么、成本、交叉印证的事实、未决问题，以及发现的文档漂移（文档 vs 代码）
   单列一节「只需修、不需裁」。

## 流程 B — 任务（之后的每一次活）

1. 读索引；只打开相关的 1–2 个线文件。不重扫仓库。
2. 说清：改动属于哪条线、模仿哪个样板成员、碰哪些配方文件、哪些规则要守、哪些已知漂移**不**抄。
3. 只打开任务需要的文件（样板成员、汇合点、配方文件、测试）。然后改真实文件、跑测试、看 diff。
4. 收尾两查：测试过；**diff 仍在线上**（同入口、同归属、同汇合点、同形状）。不在就说。
5. 只有负责人**改了设计**（数据流、归属、汇合点、形状、扩展配方）才改线。普通功能永远不改线。
   要改也是提议，不擅自动手。
6. 聊天可丢。中断时留一份很小的 `current-task.md`（做到哪 / 改了哪些文件 / 下一步）——不是记录稿。

## 流程 C — 查漂移（很多提交之后，或放 CI）

`bash scripts/check.sh <repo> <project-lines 目录>` 重跑提取器，对 `mechanical/` diff 家族、
汇合点、import、时间轴、配方；并列出基线 commit 之后新出生的成员。退出码 1 = 漂移：重读受影响的
线，只重学那一条，再让负责人确认。`churn-*` 只供参考，不参与 diff。

## 硬规则

1. 只看跟踪文件（`git ls-files`）。
2. 证据是**数出来的**（「13/35 精确形状」「汇合点引 35/35」），不是感觉。
3. 每条机制性论断引 `file:line`。没引用 → 标未核实。
4. 永远三分：标准 / 另一类 / 漂移。不合形状 ≠ 错。
5. 漂移是「别模仿」，**不是**清理任务。负责人多半答「留」。
6. 已有文档是线索；核它、列不一致。学习顺带审文档。
7. 只问会改变新代码去向的；每线 ≤5；点名成员；一问一事；代码能答的不问。
8. 设计不由模型定，由负责人定。未确认的内容不驱动代码修改。
9. 每个产出文件 ≤300 行；一线一文件。
10. 成本花在学习那一次；一个任务读 ≤2 个线文件。

## 参考数字（校准预期）

一个 150 包的 JS monorepo（5k 跟踪文件、1.3k 提交）：提取 23 秒；6 条线并行追踪约 10 分钟，
打开 ~445 个文件，~860k tokens；负责人约 30 分钟答了 23 条中的 21 条；得到 1 条新规则、5 条
「就这样」、3 条确认的漂移、5 条「留」。两处机械信号被追踪纠正（一个「噪声」文件其实是新成员的
消费者；一个 21 目录的家族实为 16 个跟踪 + 5 个未跟踪残留）——所以提取只提候选，追踪才定性。

## 文件

- `SKILL.md` / `SKILL.zh-CN.md`——本流程（英/中）
- `templates/{en,zh}/`——`index.md`、`line.md`、`questions.md`、`trace-prompt.md`
- `references/{en,zh}/`——`method.md`、`extractors.md`、`question-filter.md`、`verdicts.md`
- `scripts/`——`extract-all.sh`、`families.sh`、`hubs.sh`、`imports.sh`、`timeline.sh`、
  `recipe.sh`、`churn.sh`、`check.sh`、`lib.sh`（bash 3.2+、git、awk；语言无关）
