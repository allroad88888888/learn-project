---
name: project-lines
description: "<项目> — 已确认的主线与分支线：东西放哪、数据怎么走、模仿哪个成员、哪些漂移不抄。先读本索引，再只读相关的 1–2 个线文件。<日期> 于 commit <sha> 学得；负责人已确认的部分有标记。"
---

# <项目> · 线索引

> 由 `learn-project` 于 commit `<sha>`（<日期>）学得。下面的数字来自 `mechanical/`，可用
> `check.sh` 重算。已确认的部分标 ✅；未确认的内容不驱动代码修改。

## 怎么用
- 任务 → 在下表找到线 → 读那个文件（≤300 行）→ 模仿其样板成员、碰其配方文件、不抄其漂移 →
  收尾检查 diff 仍在线上。
- 局部任务不重扫仓库。只有负责人改了设计才改线。

## 线
| 文件 | 线 | 类型 | 汇合点（运行时 / 编辑时） | 状态 |
|---|---|---|---|---|
| `lines/00-主线-<x>.md` | … | 主线 | — | ✅ / 待 |
| `lines/10-分支-<y>.md` | `<目录>/*`（N 个成员，`<包前缀>`） | 分支线 | `<汇合点>`（n/N） | … |

**公共层（不是线）：** `<目录>/*`——N 个包，被 … 引用，无汇合点。

## 追线时发现的结构修正（≥2 条线交叉印证）
- …

## 已确认的规则（负责人 ✅）
- …

## 已确认的设计 / 已确认的漂移（别模仿）/ 答「留」的遗留
- …

## 机械证据（来自 `mechanical/`，模型没参与）
### import 方向
```
…
```
### 家族形状
```
…
```
### 时间轴 / 配方
- …

## 文档与代码不一致（只需修，不需裁）
- …

## 待确认
见 `questions.md`（N 条未决）。

## 学习成本
| 线 | 打开文件数 | tokens | 用时 |
|---|---|---|---|
| … | … | … | … |
提取：<s> 秒，零 token。
