# 线：<名字>
一句话：<这条线是什么，一句不含「和/以及」的话>
类型：主线 | 分支线——挂在主线的 `<文件>:<函数>`（运行时）与 `<文件>:<函数>`（编辑/配置时）

## 入口（一个实例从哪开始；引 file:line）
- …

## 数据怎么走（逐步；每步引 file:line）
1. 声明 → …
2. 注册 → …
3. 触发 / 渲染 → …
4. 执行 / 读写 → …
5. 结果去哪 → …

## 每部分负责什么 / 状态归谁 / 谁能调谁
| 部分 | 职责 | 持有的状态 | 谁可以调它 | 不许做 |
|---|---|---|---|---|
| … | … | … | … | … |

## 形状（分支线：目录/文件形状 + 计数；必需 vs 可选）
- 成员 N 个（git 跟踪）。精确主形状 k/N `{…}`；超集 m/N；不合形状见下。
- 必需：… 可选：…

## 样板（点名 1–2 个成员 + 为什么：奠基 / 最简 / 最近且干净）
- `<成员>`——…

## 加一个（触碰文件；每项标来源：git 配方交集 / 汇合点代码 / 已有清单；不一致处写出）
- `<文件>`——来源：…

## 标准之外
### 另一类（同目录、不同机制）
- `<成员>`——为什么是另一种东西：…
### 漂移 / 遗留（少、晚、不合形状——引用并说明；是「别模仿」不是「删」）
- `<成员或 file:line>`——…
### 待确认（≤5；只问改变新代码去向的；点名成员；每条两种解释）
1. **<题目>**（`<file:line>`）：A …；B …

## 文档与代码不一致处
- `<文档>` 说 …；代码是 …（`<file:line>`）

## 证据核过：commit `<sha>`，<日期>；本次打开文件数：N

## 裁决（负责人答复后追加——日期、人；保留问题编号）
- #n → **A / B / 都有 / 就这样 / 遗留 / 不知道**——<对新代码的含义>
